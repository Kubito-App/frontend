import styles from './loading-view.module.scss'

const LoadingView = () => {
  return (
    <div className={styles.container}>
      <span className={styles.animation}>Kubito</span>
    </div>
  )
}

export default LoadingView
